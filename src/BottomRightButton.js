import React from "react";
import Button from "react-bootstrap/Button";
function BottomRightButton({ icon }) {
  return <Button className="bottomRightButton">{icon}</Button>;
}

export default BottomRightButton;
